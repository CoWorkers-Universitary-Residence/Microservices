spring:
    application:
        name: review-service
    cloud:
        config:
            #URI where config-service is running
            uri: http://localhost:8081
            username: root
            password: universitaryresidence